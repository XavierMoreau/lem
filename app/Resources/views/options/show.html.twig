{% extends 'base.html.twig' %}

{% block body %}
    <h1>Option</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ options.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ options.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('options_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('options_edit', { 'id': options.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
